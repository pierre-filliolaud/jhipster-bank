'use strict';

angular.module('bankApp')
    .factory('Trade', function ($resource, DateUtils) {
        return $resource('api/trades/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
